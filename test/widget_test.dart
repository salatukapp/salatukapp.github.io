// Real tests live under test/unit/ and test/widget/.
// This file remains so `flutter test` doesn't complain about an empty suite.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('test suite loads', () {
    expect(true, isTrue);
  });
}
